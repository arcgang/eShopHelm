apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eshop.fullname" . }}-test-connection"
  labels:
{{ include "eshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "eshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
